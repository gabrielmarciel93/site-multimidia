const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:5173'); await page.waitForTimeout(6000); const bodyStyle = await page.evaluate(() => window.getComputedStyle(document.body).overflow); const docStyle = await page.evaluate(() => window.getComputedStyle(document.documentElement).overflow); const winHeight = await page.evaluate(() => window.innerHeight); const rootHeight = await page.evaluate(() => document.body.scrollHeight); console.log('body:', bodyStyle, 'html:', docStyle, 'height:', rootHeight, 'win:', winHeight); await browser.close();})();
