import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:5173'); await new Promise(r => setTimeout(r, 6000)); const b = await page.evaluate(() => document.body.className); const h = await page.evaluate(() => document.documentElement.className); const sty = await page.evaluate(() => window.getComputedStyle(document.body).overflow); const styY = await page.evaluate(() => window.getComputedStyle(document.body).overflowY); console.log('body classes:', b, '\nhtml classes:', h, '\nbody overflow:', sty, '\nbody overflowY:', styY); await browser.close();})();
